# Progress Tracker

Update this file after every completed feature.

## Current Status

**Project:** Sales Admin Automation Toolkit  
**Phase:** Phase 12 - Postgres-Backed Latest-Session Dashboard (complete)  
**Last completed:** Phase 12 — session-scoped, best-effort Postgres persistence layered on top of Phase 10's stateless workflow endpoints, plus a live `GET /api/dashboard` reflecting each visitor's own latest saved results with per-workflow sample-data fallback. Planned via an extensive `/grill-with-docs` session (13 resolved decisions, recorded in full in that session's transcript) plus a `/domain-modeling` pass; formalized as `docs/adr/0007-session-scoped-workflow-result-persistence.md` and new/amended `CONTEXT.md` terms (Anonymous Session ID, Saved Workflow Result, Workflow Results Store, Persistence Outcome, Dashboard Latest Results, Result Schema Version, Display Expiry). Hosting: Neon Postgres, three branches (`main`/`dev`/`test`). Backend: `X-Session-Id` header (browser UUID, no cookies) parsed by `backend/session.py`'s `get_session_id`; a single `workflow_results` JSONB table (`backend/migrations/0001_create_workflow_results.sql`), migrated via a single-transaction, advisory-lock-guarded, checksum-verified runner (`backend/db.py`) that skips cleanly with no error when `DATABASE_URL` is unset; `backend/repositories/workflow_results.py`'s `WorkflowResultsRepository` (FastAPI-dependency-injected, `save`/`get_latest`, `json.dumps(..., allow_nan=False)`-guarded, TTL + schema-version usability gate); `backend/persistence.py`'s `persist_workflow_result` helper (shared, defensive `try/except` across all three workflow routers, found necessary via the route-orchestration tests themselves); new `GET /api/dashboard` (`backend/routers/dashboard.py`, `200` all-`null` for "nothing saved"/no-DB, `503` only for a genuine outage). Frontend: `lib/session-id.ts` (`getOrCreateSessionId`), `lib/api-client.ts` extended (`X-Session-Id` on every workflow/report call, `postJSON` now returns `{data, persisted}`, new `getDashboardResults()`), `components/feedback/PersistenceNotice.tsx` (shown only on `X-Persisted: false`, on all 3 workflow pages), `components/dashboard/DashboardLiveSections.tsx` (new Client Component boundary — required because `localStorage` doesn't exist during a Server Component's Vercel-side render, a direct, documented consequence of the no-cookies session-identity decision), `app/dashboard/page.tsx` trimmed to its static shell. Tests: 2-layer suite — 15 new mocked-repository route-orchestration tests (`tests/test_backend_persistence_routing.py`, always run) + 8 new real-Neon repository/migration tests (`tests/test_workflow_results_repository.py`, `@pytest.mark.db`, skip — don't fail — when `TEST_DATABASE_URL` is unset). `uv run pytest`: **212 passing, 7 skipped**, fully hermetic with zero env vars set. `npm run typecheck`/`lint`/`build` clean; `/dashboard` still prerenders statically (the live fetch happens client-side post-hydration, not at build/request time). Live-verified against the real Neon `dev` branch and via Playwright in a real browser: fresh-visitor sample fallback with "Sample data" chips, a workflow run updating the dashboard on next load with the chip gone for that section only, other untouched sections still sample, and a `localStorage.clear()` returning to full sample state. A real secret-leak near-miss was caught and fixed mid-session: real Neon credentials were briefly written to the committed `.env.example` instead of the gitignored `.env` — caught before anything was staged or committed; moved to `.env` (confirmed gitignored) and `.env.example` restored to its empty-template convention.  
**Also completed earlier:** Phase 11 (Deployment Baseline) — the app is live in production on two independent hosted services, deployed from a dedicated `deploy/portfolio-demo` branch (currently fast-forwarded to commit `974b348`, tracking `phase/10-fastapi-integration`). Backend deployed to Render (Free Web Service, Python runtime, build `uv sync --frozen --no-dev && uv cache prune --ci`, start `uv run fastapi run backend/main.py --host 0.0.0.0 --port $PORT`, Health Check Path `/health`): **https://sales-ops-6e84.onrender.com**. Frontend deployed to Vercel (Hobby tier): **https://sales-ops-gamma.vercel.app/**. Repo changes landed in two commits on `phase/10-fastapi-integration` (updating existing PR #9, no new PR): `backend/main.py` gained env-driven `CORS_ALLOWED_ORIGINS` (comma-separated, trimmed, empty-entries filtered) and a minimal `GET /health` returning `{"status": "ok"}`; new `.env.example` (required adding a `!.env.example` negation line to `.gitignore`, since the blanket `.env*` rule was silently excluding it — caught via `git status`/`git check-ignore` before it went unnoticed); `README.md` gained a "Live Demo" section (with a Render cold-start note) later filled in with the real verified URLs; `context/build-plan.md`/`context/architecture.md` updated with the two-service deployment shape; `CLAUDE.md`'s stale "Candidate next scope" pointer (previously pointing at the paused SQLite plan file) corrected. **Verification method — live, direct `curl`, not assumed:** backend `GET /health` → `200 {"status":"ok"}`; frontend `/` → `307` redirect to `/dashboard` (expected root-redirect behavior) → `200` with real rendered content (`<title>Sales Admin Automation Toolkit</title>` plus live dashboard chart titles matching the known dataset: 26 fully/1 partially/1 backordered); a live `OPTIONS` CORS preflight from the Vercel origin against the Render backend returned `access-control-allow-origin: https://sales-ops-gamma.vercel.app` exactly matching (a trailing-slash bug in the configured origin was caught and fixed before this check, since browsers never send a trailing slash in the `Origin` header). `uv run pytest` 196 passing, unchanged — no Python business logic touched, only the two `backend/main.py` lines above. This phase also folded in Phase 10.2's previously-uncommitted UI polish work (see the Phase 10.2 entry below), committed in the same session ahead of the deployment-prep commit.
**Also completed since Phase 11:** a named, non-phase-numbered **Mobile Nav/Shell Responsiveness** pass (planned via `/architect`) fixed the Phase 10.2-flagged limitation where the fixed `w-60` sidebar squeezed main content on mobile viewports — now live in production. See the "Mobile Nav/Shell Responsiveness" entry below for detail.
**Next:** no phase is currently planned. The paused SQLite work (`git stash@{0}`) remains intact and unaffected — Phase 12 superseded it for dashboard purposes without reusing it; its fate (delete vs. keep as historical reference) is still an open question. Whether/when to merge the stacked PR chain remains the user's call, unchanged across every phase since Phase 4.

## Important Note

Previous JobPilot planning and progress are no longer active for this project. Treat those decisions as superseded. The active project is the Sales Admin Automation Toolkit.

The previous static-UI-first decision is also superseded. The active build order is Python business logic and output contracts first, polished Next.js dashboard second.

Phase 7 is allowed to start after Phase 2 because it is planning-only. Phase 8 is hard-gated on passing Phase 3-6 tests.

## Progress

### Phase 0 - Planning Reset

- [x] Rewrite context files for Sales Admin Automation Toolkit
- [x] Add root `CONTEXT.md` domain glossary
- [x] Add ADR: Next.js V2 first instead of Streamlit V1
- [x] Add ADR: static mock UI before Python/FastAPI integration
- [x] Add docs for context reset
- [x] Add ADR: Python core before polished UI
- [x] Revise context files for Python-first sequence

### Phase 1 - Python Project Foundation

Tooling conventions locked via `/grill-with-docs` session, see `docs/adr/0004-phase-1-python-tooling.md`: Python 3.12, `pyproject.toml` + `uv`, pytest config in `pyproject.toml`, no placeholder modules for later phases.

- [x] Create Python project config (`pyproject.toml`, `.python-version` = 3.12)
- [x] Create `src/` package (`__init__.py`, `excel_io.py`, `contracts.py` only)
- [x] Create `tests/` package (`test_excel_io.py`, `test_contracts.py` only)
- [x] Create `sample_data/` folder (empty)
- [x] Add pytest configuration (`[tool.pytest.ini_options]` in `pyproject.toml`)
- [x] Add README setup notes (`uv sync`, `uv run pytest`)
- [x] Add `src/excel_io.py` (load helper, required-column validation, missing-column error shape)
- [x] Add `src/contracts.py` (TypedDicts for all 13 output families incl. `PaymentDataIssueRow`, no business logic)

### Phase 2 - Sample Data and Contract Fixtures

- [x] Generate `sample_orders.xlsx` (mostly valid, 1 duplicate order ID, 1 invalid SKU)
- [x] Generate `sample_product_master.xlsx` (mostly active SKUs, 1 inactive SKU)
- [x] Generate `sample_inventory.xlsx` (limited stock for MED-LENS-001 across 2 warehouses, 1 SKU below reorder point)
- [x] Generate `sample_invoices.xlsx` (mix of current/paid/partial/overdue, 1 high-priority overdue, 1 missing-due-date data-issue row)
- [x] Populate validation contract fixtures with realistic sample values
- [x] Populate allocation contract fixtures with realistic sample values
- [x] Populate payment aging contract fixtures with realistic sample values
- [x] Populate report manifest fixture with realistic sample values

### Sample Data Enrichment (Phase 2 revisit)

- [x] Fictional regional medical optics/healthcare equipment scenario (HK + Mainland China customers, HK/China/Europe warehouses)
- [x] `sample_product_master.xlsx` — 18 SKUs, 2 inactive
- [x] `sample_orders.xlsx` — 36 rows, 7 issue-bearing (~19%), several combined-issue rows
- [x] `sample_inventory.xlsx` — 17 rows across 3 warehouses, guaranteed partial/backorder/reorder-alert cases
- [x] `sample_invoices.xlsx` — 27 rows, all 5 aging buckets, both data-issue types, 1 deliberate overpayment
- [x] `sample_customers.xlsx` — new, optional/reference-only, 17 rows
- [x] `sample_data/README_sample_data.md` — new
- [x] `tests/test_sample_data.py` rewritten for new dataset + new issue coverage
- [x] End-to-end compatibility check against `order_validation.py`/`inventory_allocation.py`/`payment_aging.py`/`report_export.py`

### Phase 3 - Order Validation Core

- [x] Required column checks
- [x] Required field validation
- [x] Duplicate order ID detection
- [x] Valid active SKU checks
- [x] Positive whole-number quantity checks
- [x] Delivery date validation
- [x] Priority controlled values
- [x] Payment terms check
- [x] pytest coverage

### Phase 4 - Inventory Allocation Core

- [x] Allocation sort order
- [x] Allocatable quantity calculation
- [x] Full allocation
- [x] Partial allocation
- [x] Backorder handling
- [x] Non-negative inventory guarantee
- [x] Warehouse choice rule
- [x] Reorder alert / supplier follow-up
- [x] pytest coverage

### Phase 5 - Payment Aging Core

- [x] Outstanding amount calculation
- [x] Paid invoice handling
- [x] Days overdue calculation
- [x] Aging bucket assignment
- [x] Follow-up priority assignment
- [x] Missing due-date issues
- [x] Invalid amount issues
- [x] Draft reminder generation
- [x] pytest coverage

### Phase 6 - Excel Report Export

- [x] Order validation report
- [x] Inventory allocation report
- [x] Payment aging report
- [ ] Optional full operations report pack — deferred pending ADR. `operations_follow_up_pack.xlsx` is labeled V1.5 in `05_integration_and_app_flow.md` §7, not V1; per the Scope Gate it needs a new ADR before implementation, not just a build-plan checkbox.
- [x] pytest coverage for workbook structure
- [x] Fallback demo milestone: tested logic + professional `.xlsx` reports are interview-ready

### Phase 7 - UI Contract and Wireframe Planning

- [x] Gate check: Phase 2 contract fixtures complete
- [x] TypeScript interface plan from Python outputs
- [x] Route/page plan
- [x] Table column plan
- [x] KPI and chart mapping
- [x] Figma/MCP reference review if useful — deferred in Phase 7 (no connector then); actually performed in Phase 8 once the Figma MCP was connected. Four Figma Make prototypes inspected and classified in `ui-contract-plan.md`'s Figma Reference Reconciliation section.

### Phase 8 - Next.js Frontend Foundation

- [x] Gate check: all spec-listed pytest cases pass for Phases 3-5 — line-by-line audit, all 26 spec §11/§12 cases map to named tests (8 order-validation, 8 inventory-allocation, 10 payment-aging)
- [x] Gate check: Phase 6 Excel report structure tests pass — 24/24 in `test_report_export.py`
- [x] Scaffold Next.js App Router project — Next 16 + React 19 at repo root, transplanted from a scratchpad scaffold so no existing Python/context/docs files were clobbered
- [x] Configure TypeScript strict — `strict: true`, `@/*` path alias, `typecheck` script (`tsc --noEmit`)
- [x] Configure Tailwind CSS 3.4 tokens — pinned 3.4.19 (no v4), `tailwind.config.ts` + `globals.css` wired to `ui-tokens.md` tokens verbatim, Inter via `next/font`
- [x] Add shadcn/ui base setup — plumbing only: `cn()` (`lib/utils.ts`), `components.json`, deps (clsx/tailwind-merge/cva/lucide). Primitive components and any shadcn-token bridge deferred to Phase 9 (see Decisions Made) to avoid injecting a token set that collides with `ui-tokens.md`
- [x] Add app shell routes — 5 stub routes (`/dashboard`, `/order-validation`, `/inventory-allocation`, `/payment-aging`, `/reports`) + root redirect to `/dashboard`; real `AppShell`/`SidebarNav` and page content are Phase 9

### Phase 9 - Reusable UI Components and Static Pages

- [x] AppShell
- [x] SidebarNav
- [x] TopHeader
- [x] MetricCard
- [x] WorkflowStepper
- [x] UploadPanel
- [x] StatusBadge
- [x] DataTable
- [x] ReportCard
- [x] EmptyState
- [x] LoadingState
- [x] BusinessErrorMessage
- [x] Dashboard
- [x] Order Validation
- [x] Inventory Allocation
- [x] Payment Aging
- [x] Reports

### Phase 9.1 - Visual Alignment Fixes

- [x] Visual alignment review (code + rendered-HTML audit, cross-checked against 4 Figma Make prototypes)
- [x] Critical: `AppShell`/`SidebarNav` height model fix
- [x] Brand name consistency, `/reports` width fix, `WorkflowStepper` terminal-step color
- [x] Download actions on 3 workflow pages, Payment Aging "As of" date
- [x] Icon-chip `MetricCard`, zebra-striped `DataTable`
- [x] Table density/typography/truncation/sort-coverage pass (all 3 workflow pages)
- [x] `FilterToolbar`/`FilterSelect`/`MiniBar` — filtering and sorting restored to `DataTable` via page-level composition
- [x] `TableSectionHeading` icon+caption pattern across all tables/panels
- [x] Dashboard: Inventory Shortage Alerts, Payment Follow-up Items, workflow infographic, "How This Demo Works" guide (English only)
- [x] `DonutBreakdownChart`/`VerticalBucketBarChart` (`components/charts/`) replacing dashboard mini-bar visuals
- [x] Before/after manual screenshot verification (Figma MCP rate-limited mid-session; fell back to developer-provided screenshots per the documented fallback)

### Phase 10 - FastAPI Integration

- [x] FastAPI endpoints (`backend/`: orders, inventory, payments, templates routers + stateless report endpoints)
- [x] Frontend API adapter (`lib/api-client.ts`)
- [x] Contract tests (`tests/test_backend_*.py`, 22 new, `TestClient` against real `sample_data/*.xlsx`)
- [x] End-to-end workflow checks (live-browser verification, both dev servers, all 3 workflows + error case)

### Phase 10.2 - Portfolio UI Polish

- [x] Inverse Surface token family (`app/globals.css`, `tailwind.config.ts`, `context/ui-tokens.md`)
- [x] `SidebarNav` dark-navy restyle + solid-accent active state + per-item icons
- [x] `Button` `dark` variant (Download Report, Sample file)
- [x] `MetricCard` compact-tile restructure + hover/shadow
- [x] `UploadPanel` bottom-anchored drop zone + Sample file button sizing fix
- [x] `/dashboard` KPI consolidation into one 5-card Overview row
- [x] `/dashboard` chart-card sizing fix, action links, hover, data-bearing tooltips
- [x] `/dashboard` Inventory Shortage Alerts / Payment Follow-up Items paired side by side
- [x] Workflow-page KPI grids fit each page's own card count in one row
- [x] Hydration-mismatch and pointer-events bugs found and fixed
- [x] `context/ui-registry.md` updated for every touched component + Phase 10.2 page composition notes

### Phase 11 - Deployment Baseline

- [x] Env-driven `CORS_ALLOWED_ORIGINS` + `GET /health` on `backend/main.py`
- [x] `.env.example` template committed (`.gitignore` fixed to stop silently excluding it)
- [x] Backend deployed to Render (Free Web Service) — https://sales-ops-6e84.onrender.com
- [x] Frontend deployed to Vercel (Hobby tier) — https://sales-ops-gamma.vercel.app/
- [x] `deploy/portfolio-demo` branch created and kept fast-forwarded from `phase/10-fastapi-integration`
- [x] `README.md` Live Demo section filled in with real, verified URLs
- [x] `context/build-plan.md`/`context/architecture.md` updated with two-service deployment shape
- [x] Live end-to-end verification via direct curl: health check, root redirect + rendered content, CORS preflight

### Mobile Nav/Shell Responsiveness

- [x] New `--overlay` token (`app/globals.css`, `tailwind.config.ts`, `context/ui-tokens.md`)
- [x] `AppShell` promoted to Client Component, holds `isDrawerOpen` state
- [x] Desktop sidebar wrapper (`hidden lg:block`) unchanged in behavior from before this pass
- [x] Mobile drawer wrapper + backdrop, conditionally mounted only while open (not tabbable when closed)
- [x] Drawer closes on backdrop click, `Escape`, and automatic route-change
- [x] `TopHeader` promoted to Client Component, owns the `lg:hidden` menu toggle button
- [x] `context/ui-registry.md` updated (`AppShell`/`SidebarNav`/`TopHeader` entries + new page composition notes), Phase 10.2 known-limitation note resolved
- [x] `npm run typecheck`/`lint`/`build` clean
- [x] Live-browser verification at mobile + desktop widths, plus a keyboard-only Tab/Escape pass

### Phase 12 - Postgres-Backed Latest-Session Dashboard

- [x] `/grill-with-docs` session (13 resolved decisions) + `/domain-modeling` pass
- [x] `docs/adr/0007-session-scoped-workflow-result-persistence.md`; `CONTEXT.md` new/amended terms
- [x] `docs/archive/phase-11-sql-reporting-sqlite-plan.md` (archived the superseded SQLite plan)
- [x] Neon Postgres project + `main`/`dev`/`test` branches
- [x] `backend/migrations/0001_create_workflow_results.sql`; `backend/db.py` (single-transaction, advisory-lock, checksum-verified migration runner; `DATABASE_URL`-optional pool)
- [x] `backend/session.py` (`get_session_id`); `backend/repositories/workflow_results.py` (`WorkflowResultsRepository`, FastAPI-injected); `backend/persistence.py` (`persist_workflow_result`)
- [x] `src/contracts.py`'s `CONTRACT_SCHEMA_VERSIONS`
- [x] `orders.py`/`inventory.py`/`payments.py` routers wired for best-effort persistence + `X-Persisted`; report endpoints unchanged
- [x] `backend/routers/dashboard.py` (`GET /api/dashboard`); `backend/main.py` lifespan hook + CORS `expose_headers`
- [x] `lib/session-id.ts`; `lib/api-client.ts` extended; `types/dashboard.ts`
- [x] `components/feedback/PersistenceNotice.tsx`; wired into all 3 workflow pages
- [x] `components/dashboard/DashboardLiveSections.tsx`; `app/dashboard/page.tsx` trimmed to static shell
- [x] `MetricCard` gained an optional `sample` chip prop
- [x] `tests/test_backend_persistence_routing.py` (15, mocked repo); `tests/test_workflow_results_repository.py` (8, `@pytest.mark.db`, real Neon `test` branch)
- [x] `pyproject.toml` `db` marker registered
- [x] `.env.example`/`.env` secret-leak caught and fixed mid-session
- [x] `uv run pytest` 212 passing/7 skipped hermetically; `npm run typecheck`/`lint`/`build` clean
- [x] Live-browser verification (Playwright) + real-Neon smoke tests (save/read/upsert/version-gate/TTL)
- [x] `context/architecture.md`/`context/build-plan.md`/`context/progress-tracker.md`/`CLAUDE.md`/`context/ui-registry.md` updated

## Decisions Made

- The active project is Sales Admin Automation Toolkit, not JobPilot.
- First implementation target is Python business-rule core.
- The polished UI target remains Next.js V2, but after Python outputs are stable.
- UI contract/wireframe planning may run after Phase 2 and in parallel with Python business-rule implementation.
- Real Next.js implementation must wait until all spec-listed Python tests and Excel report structure tests pass.
- Streamlit is not the active UI implementation path.
- FastAPI comes after the Python core and UI contracts are stable.
- CRM Cleaner is postponed.
- Optional/V1.5/V2 spec items are out of scope unless a new ADR explicitly adds them.
- Legacy JobPilot design images remain in `context/designs/` only as archived assets and are not active UI references.
- Phase 1 tooling locked: Python 3.12, `pyproject.toml` + `uv` (no `requirements.txt`), pytest config in `pyproject.toml`, no placeholder files for later-phase modules (`docs/adr/0004-phase-1-python-tooling.md`).
- Added `PaymentDataIssueRow` as a 13th output-contract family: the payment-aging Data Issues sheet (PA-006/PA-007) had no matching contract in the original 12-family list (`docs/adr/0005-payment-data-issue-row-contract.md`).
- Phase 1 complete: `pyproject.toml`/`uv` project config, `src/excel_io.py`, `src/contracts.py` (13 output families), `tests/test_excel_io.py`, `tests/test_contracts.py` all in place; `uv run pytest` passes (7 tests).
- Phase 2 tooling decisions (via `/architect`): `sample_invoices.xlsx` due dates are generated as offsets from a `reference_date: date | None = None` parameter (resolved to `date.today()` inside the function, not a literal default) so the aging demo stays believable whenever regenerated; orders/inventory use plain fixed dates since their rules don't depend on "today". Contract fixtures live in `tests/contract_fixtures.py` (not inline in tests, not a new `src/` module) so Phase 7 UI planning can read them directly.
- Phase 2 complete: `src/sample_data.py`, `sample_data/*.xlsx` (4 files), `tests/contract_fixtures.py`, `tests/test_sample_data.py`, extended `tests/test_contracts.py`; `uv run pytest` passes (32 tests).
- Phase 3 decisions (via `/architect`): `validate_orders()` returns a single `OrderValidationResult` dict envelope (`summary`/`valid_orders`/`errors`), defined locally in `order_validation.py`, not added to `contracts.py`. Multiple validation errors allowed per row; OV-001 emits one error per missing field. Resolved an internal spec contradiction (OV-001 vs OV-007 both covering blank `payment_terms`) in favor of OV-007-Warning-only, so Warning-only rows stay valid. Blank fields skip their downstream value-quality rule (OV-002/003/004/005/006) to avoid double-flagging. OV-003 split into `UNKNOWN_SKU`/`INACTIVE_SKU`/`INVALID_ACTIVE_FLAG`; OV-005 split into `INVALID_ORDER_DATE`/`INVALID_DELIVERY_DATE`/`DELIVERY_BEFORE_ORDER`. Malformed quantity/dates convert to business-readable errors rather than raising. `product_name` fills from product master only when the order row's own value is blank.
- Phase 3 complete: `src/order_validation.py`, `tests/test_order_validation.py`; `uv run pytest` passes (63 tests).
- Phase 4 decisions (via `/architect`): `allocate_inventory()` returns a local `InventoryAllocationResult` dict envelope (`summary`/`allocation_results`/`backorders`/`remaining_inventory`/`supplier_follow_ups`), defined locally in `inventory_allocation.py`, not added to `contracts.py` — mirrors Phase 3's envelope pattern. IA-007 V1 warehouse choice compares `allocatable_qty` (`available_qty - reserved_qty`, floored at 0), not raw `available_qty`; ties broken by warehouse name ascending. Allocation is stateful and per-line: orders processed strictly in IA-001 order, one line fulfilled from exactly one warehouse, with warehouse choice re-evaluated per line as stock depletes. A fully-backordered line still records the best-candidate warehouse when the SKU has any inventory rows; `warehouse` is `""` only when the SKU has zero inventory rows anywhere. Supplier Follow-up is scoped strictly to IA-008's literal trigger (`reorder_point` present and `remaining_qty` strictly below it, evaluated once per (sku, warehouse) after the full batch) — not triggered by backorder status alone, despite `CONTEXT.md`'s broader glossary phrasing. `remaining_qty = starting_available_qty - allocated_qty` (not `allocatable_qty`, since `reserved_qty` only gates allocation eligibility). `low_stock_sku_count` counts distinct SKUs with at least one alerting (sku, warehouse) row, not follow-up row count. Malformed/blank required inventory values (`sku`, `warehouse`, `available_qty`) raise a new `InvalidInventoryDataError` rather than silently coercing, since no `InventoryDataIssueRow` contract exists to report them as soft errors; optional numeric fields (`reserved_qty`, `reorder_point`, `lead_time_days`) still degrade gracefully to "not present" when blank/unparseable.
- Phase 4 complete: `src/inventory_allocation.py`, `tests/test_inventory_allocation.py`; `uv run pytest` passes (93 tests). Sanity-checked against `sample_data/sample_orders.xlsx` + `sample_data/sample_inventory.xlsx` (routed through Phase 3's `validate_orders()` first) — output matches the hand-authored fixtures in `tests/contract_fixtures.py` field-for-field. Post-review hardening added business-readable direct-call failures for malformed valid-order inputs, negative optional inventory quantities, and offending cell values in inventory error messages.
- Phase 5 decisions (via `/architect`): `calculate_payment_aging()` returns a single local `PaymentAgingResult` dict envelope (`summary`/`aging_rows`/`data_issues`/`draft_messages`), defined locally in `payment_aging.py`, not added to `contracts.py` and not split into the spec's suggested multi-function/DataFrame-tuple shape — mirrors Phase 3/4's envelope pattern. Paid invoices (`outstanding_amount <= 0`, clamped, never negative) stay in `aging_rows` with `follow_up_priority="None"` and `suggested_action="No action required"` — excluded only from active follow-up (High/Medium/Low/Watch) and draft messages, not from the aging table itself. `total_invoices` counts every loaded row including PA-006/PA-007 data-issue rows; `aging_bucket_counts`/`total_outstanding_amount`/`overdue_amount`/`high_priority_count` are computed only from `aging_rows`, mirroring Phase 3's `total_orders` semantics. `as_of_date: date | None = None` resolves to `date.today()` inside the function body (never a literal default), matching the Phase 2 `sample_data.py` convention. `days_overdue` is the raw signed value (`effective_date - due_date`), negative for future due dates, letting Watch be derived as `-7 <= days_overdue <= 0`. Draft reminders are generated only when `outstanding_amount > 0 and days_overdue > 0 and follow_up_priority in {High, Medium, Low}` — never for Watch/Current/Paid/data-issue rows. Invalid/missing `paid_amount` (blank, non-numeric, or negative) silently degrades to `0.0` with no data issue, since PA-007 only names `invoice_amount`; only `invoice_amount` missing/non-numeric/negative triggers a PA-007 data issue, and a row can carry both a PA-006 and PA-007 issue independently. Draft message amounts are formatted with the invoice's own `currency` column when present (e.g. `"HKD 58,000.00"`), falling back to a bare `"58,000.00"` when blank — not the literal `$` the original hand-authored fixture used, since currency is not carried into any output contract per the Field Scope Boundary and a bare `$` would misrepresent HKD/SGD/TWD sample invoices. `PaymentDataIssueRow` does not surface `row_number`; it stays an internal-only loop index, matching the existing contract shape.
- Phase 5 complete: `src/payment_aging.py`, `tests/test_payment_aging.py`; `uv run pytest` passes (127 tests). Sanity-checked against `sample_data/sample_invoices.xlsx` (`as_of_date=2026-07-09`) — `INV-2026-001`'s computed row matched `tests/contract_fixtures.py`'s `PAYMENT_AGING_ROW_FIXTURE` field-for-field after correcting a pre-existing 1-day date typo in that fixture (`invoice_date`/`due_date` were off by one from the real generated sample data) and updating `DRAFT_MESSAGE_ROW_FIXTURE`'s hardcoded `$58,000.00` to the currency-aware `HKD 58,000.00`. Before starting Phase 5, committed substantial uncommitted Phase 4 hardening work found already sitting in the working tree (new `InvalidOrderDataError` for malformed valid-order fields, negative-quantity rejection on optional inventory fields, direct column validation inside `allocate_inventory()`) to PR #3 first, per user instruction, bringing Phase 4's test count from 85 to 93 before branching Phase 5.
- Phase 7 decisions (via `/architect` + `AskUserQuestion`): TypeScript interfaces are literal `.ts` code blocks in `context/ui-contract-plan.md` (new), snake_case fields preserved verbatim, no camelCase adapter decided. `context/ui-rules.md`'s Status Badges section rewritten in full for all 4 workflow groups (not just the 2 originally flagged) into an explicit direct-vs-derived structure — Inventory Allocation and Payment Aging were also mixing controlled-vocabulary fields with unexplained derived shorthand. `/dashboard` scoped as a read-only aggregate landing page (per-workflow KPI strips with independent empty states + 3 report cards + 3 entry cards), no persisted cross-workflow state, no new contract. Client-side derived aggregates permitted for charts/KPIs (e.g. "90+ Days Amount", "Backordered Qty by SKU") as long as each is explicitly labeled with its source rows/fields/grouping rule. `CONTEXT.md` not edited this phase — `Status Badge`/`Derived Display Value` are UI-planning terms, defined in `ui-contract-plan.md` instead. Figma/MCP reference deferred (none available this session). Discovered and flagged a `ReportManifest.report_id` format mismatch between `tests/contract_fixtures.py` (stale) and the real `report_export.py` exporter; corrected after Phase 7 before Phase 8 prep.
- Post-Phase-7 fixture correction complete: `REPORT_MANIFEST_FIXTURES.report_id` now uses the real timestamp-based exporter format for all 3 report types, and `tests/test_contracts.py` verifies each fixture ID matches its own `report_type` + `generated_at`.
- Phase 7 complete: `context/ui-contract-plan.md` (new), `context/ui-rules.md` (Status Badges section rewritten), `context/progress-tracker.md` updated; no `src/`/`tests/` changes, `uv run pytest` still passes (151 tests, unchanged).
- Phase 8 decisions (via `/architect` + `AskUserQuestion`, Figma MCP now connected): scaffolded into a scratchpad temp dir and transplanted only Next.js files to the repo root, because `create-next-app` generates its own `AGENTS.md`/`CLAUDE.md`/`README.md`/`.gitignore` that would clobber the real ones — `git status` confirmed no Python/context/docs file was touched. Tailwind pinned to 3.4.19 (create-next-app defaults to v4); set up via the v3 path (`tailwind.config.ts` + `@tailwind` directives + autoprefixer), not v4's CSS-first config. Snake_case preserved verbatim in `types/index.ts` and mock JSON — no camelCase adapter (carried from Phase 7). Payment Aging badges stay direct-fields-only (`aging_bucket`, `follow_up_priority`); no Paid/Overdue shorthand. **shadcn is plumbing-only this phase** (`cn()`, `components.json`, deps) — primitive components deferred to Phase 9. Reason: `ui-tokens.md`'s token names are authoritative and collide with shadcn's default set (notably `--accent` = brand blue here vs shadcn's gray hover); adding primitives now would force injecting shadcn's parallel tokens, which the CRITICAL "never add tokens without approval" rule (`skills/tailwind-best-practices`) forbids. Phase 9 adds each primitive only when a real component needs it, reconciles its classes to `ui-tokens.md`, and `/imprint`s the pattern. Mock JSON is generated build-time-only by `scripts/generate_mock_data.py` from `tests/contract_fixtures.py`; the Next.js app never imports `tests/` at runtime — it reads committed `lib/mock-data/*.json`.
- Phase 8 complete: `app/` (root layout + 5 stub routes + root redirect), `tailwind.config.ts`, `postcss.config.mjs`, `app/globals.css` (tokens), `lib/utils.ts`, `components.json`, `types/index.ts`, `scripts/generate_mock_data.py`, `lib/mock-data/*.json`, `package.json`/lockfile, merged `.gitignore`. `npm run build`/`lint`/`typecheck` clean; all 5 routes serve 200 with semantic-token CSS compiled; `uv run pytest` still 152 (Python untouched). `context/ui-registry.md` intentionally left empty — no reusable components built this phase.
- Phase 9 decisions (via `/architect` + `AskUserQuestion`): static showcase, not a simulated live app — pages render `lib/mock-data.ts`'s typed mock data immediately, `UploadPanel` is a real file-picker that never gates content, `ReportCard` renders `Ready` straight from the mock `ReportManifest`, and the other 3 report-lifecycle states plus `WorkflowStepper`'s steps exist only as prop-driven visual variants, never live-wired via timers. `DataTable` is plain client-side single-column sort with no new dependency (no TanStack Table this phase — mock fixtures are 1-2 rows). All primitives (`Button`/`Card`/`Badge`/`Table`) are hand-written under `components/ui/`, never generated via `npx shadcn add`, because `components.json`'s `baseColor: "slate"` would inject shadcn's own colliding tokens into `globals.css`. No Recharts — the Payment Aging aging-bucket breakdown is a token-styled bar list. `StatusBadge`'s status→tone mapping was fixed before build (see `context/ui-registry.md`'s StatusBadge entry), not decided ad hoc. Added `lib/mock-data.ts` and `lib/formatters.ts` per `architecture.md`'s already-documented Future Frontend Shape.
- Phase 9 build-time discovery (not anticipated in planning): React Server Components cannot pass functions as props to Client Components. `DataTable`'s `columns` config carries `render`/`sortValue` functions, so every page that builds a `DataTable` column config must itself be a Client Component, not just `DataTable` — `order-validation`, `inventory-allocation`, and `payment-aging` pages are `"use client"` for this reason (discovered via a real `next build` prerender failure, not a review pass). `dashboard` and `reports` don't use `DataTable` and stay Server Components as originally planned.
- Phase 9 complete: 12 registry components + 4 hand-written primitives (`components/ui/`, `components/layout/`, `components/feedback/`, `components/workflow/`, `components/tables/`), `lib/mock-data.ts`, `lib/formatters.ts`, all 5 routes filled in against mock JSON. `npm run build`/`lint`/`typecheck` clean; `uv run pytest` still 152 (Python untouched); route smoke checks confirm real contract data (not placeholder text) renders on all 5 routes; token/raw-color scan and `globals.css` diff (no shadcn-default variables introduced) both clean; Figma Reference Reconciliation corrections (real priority vocab, real allocation status values, real $50k threshold, real supplier-follow-up field set, verbatim draft-message text, fixed 5-route nav) verified present, not the Figma prototypes' invented versions.
- Phase 9.1 decisions (via `/project-review` + `/architect` + `AskUserQuestion`, multiple review passes in one session): the sidebar-height bug (`SidebarNav`'s `h-screen` inside `AppShell`'s uncapped `min-h-screen` shell — sidebar stops at one viewport while content grows taller) was Critical and confirmed only via rendered-HTML class inspection, not source reading alone; fixed by capping the whole shell to `h-screen overflow-hidden` (matching the pattern found in the Figma Overview Dashboard prototype's own root) and making `<main>` the sole internal scroll region. Filtering was reintroduced to `DataTable` as page-level composition (`FilterToolbar`/`FilterSelect` filter the `data` array before it reaches `DataTable`) rather than teaching `DataTable` about filters itself — revises Phase 9's "sort only, no filtering" decision without growing `DataTable`'s own prop surface. `StatusBadge`'s `Tone` type is imported into the new `components/charts/` components as a type-only import from `components/workflow/StatusBadge.tsx` (matching the existing `MiniBar`/`SegmentedBar` precedent) — the actual `allocationStatusTone`/`agingBucketTone` tone-resolution functions are called only at page level, so charts never decide their own colors and can't drift from what table badges show for the same status. All tone→class mappings across every new component (`Badge`'s dot, `MiniBar`, `SegmentedBar`, `DonutBreakdownChart`, `VerticalBucketBarChart`) are literal `Record<Tone, string>` objects — never dynamic template-string class construction — so Tailwind's static scanner always finds them. Bilingual (Traditional Chinese) guide content and a second per-column header-filter mechanism (distinct from the toolbar filters) were both explicitly deferred, not built.
- Phase 9.1 complete: Critical sidebar fix; brand/width/stepper-color fixes; download actions + Payment Aging date; icon-chip `MetricCard` + zebra-striped `DataTable`; full table density/typography/truncation/sort-coverage pass; `FilterToolbar`/`FilterSelect`/`MiniBar`/`TableSectionHeading` (new `components/tables/` additions); `/dashboard` gained Inventory Shortage Alerts, Payment Follow-up Items, a workflow infographic, and an English-only guide section; `DonutBreakdownChart`/`VerticalBucketBarChart` (new `components/charts/` folder) replaced the dashboard's Allocation/Payment-Aging mini-bar visuals, sourced from 2 new `ui-contract-plan.md` derived aggregates ("Gap to Reorder Point," "Outstanding amount by aging bucket"). `npm run build`/`lint`/`typecheck` clean and `uv run pytest` still 152 at every checkpoint across the whole phase; before/after manual screenshots (Figma MCP capture hit its Starter-plan rate limit mid-session, 3 of 5 "before" routes captured before falling back to developer-provided screenshots per the documented fallback) confirm every fix rendered correctly with no regressions.
- Sample Data Enrichment decisions (via built-in Plan Mode acting as the `/architect` step, + `AskUserQuestion` + a user follow-up correction before implementation): scenario fictionalized as a regional medical optics/healthcare equipment company "inspired by a ZEISS Far East-style operating model" (tonal/structural inspiration only — no real company name, trademark, product line, pricing, or confidential detail used anywhere in generated data, code, or docs). Generation stays hand-authored literal rows in `src/sample_data.py` (no `random`/seed module) — README states "deterministic by construction," not a random-seed strategy, resolving a wording mismatch with the planning docs. Orders hold ~19% issue-bearing rows by combining multiple intentional issues onto the same row (e.g. missing SKU + blank `payment_terms` together) rather than one row per issue type. `sample_invoices.xlsx` gained the optional `order_id` column (mostly references a real generated order, non-functional context, not in `payment_aging.py`'s required columns). `sample_customers.xlsx` generated but strictly reference-only — no loader/validation function anywhere in `src/`. No output contract, business rule, or UI/`app`/`components`/`types`/`lib` file changed. `unit_price`/`default_supplier` product-master columns and `customer_id`-based validation from the planning docs were rejected as out of scope (unused by any business module; `reorder_point` already lives per-warehouse in inventory).
- Sample Data Enrichment complete: `src/sample_data.py` rewritten (18 products/36 orders/17 inventory rows/27 invoices/17 customers), `sample_data/README_sample_data.md` (new), `sample_data/sample_customers.xlsx` (new), all 5 `sample_data/*.xlsx` regenerated, `tests/test_sample_data.py` rewritten (32 tests, up from 11). `uv run pytest` passes (172 tests, up from 152). End-to-end compatibility verified by running the real `validate_orders`/`allocate_inventory`/`calculate_payment_aging`/`report_export` pipeline against the newly generated files (28 valid/8 invalid orders spanning 8 distinct error codes; 26 fully/1 partially allocated/1 backordered + 3 supplier follow-ups; all 5 aging buckets + both data-issue types + 17 draft messages; all 3 Excel reports export without error).
- Mock-data pipeline reconnection decisions (via `/architect`, same session, user-directed "Option C"): `tests/contract_fixtures.py` stays exactly as-is — small, single-row, contract-shape fixtures for `test_contracts.py`/`test_report_export.py` only, never touched by or coupled to the UI mock-data pipeline again. `scripts/generate_mock_data.py` was rewritten in place (not a second script) to become the single sanctioned generator of `lib/mock-data/*.json`, reading `sample_data/*.xlsx` via each business module's own `load_*` helper (the same code path a real upload exercises) and running the real `validate_orders`/`allocate_inventory`/`calculate_payment_aging`/`report_export` pipeline — framed in its own docstring as a compatibility smoke test: a failure here means the committed sample Excel no longer matches what the Python loaders/business modules expect, not routine noise. `MOCK_AS_OF_DATE = date(2026, 7, 10)` and `MOCK_GENERATED_AT = datetime(2026, 7, 10, 9, 0, 0)` are fixed module-level constants (matching `sample_data/README_sample_data.md`'s stated generation date) passed into `calculate_payment_aging`/`report_export` so the committed JSON's aging buckets and `report_id`/`generated_at` values are deterministic across re-runs, not dependent on whatever day the script happens to run. `sample_customers.xlsx` is never read by this script — reference-only status holds all the way through the UI layer. `package.json` gained a manual-only `"mock-data"` alias, not wired into `build`/`lint`/`typecheck`. `lib/mock-data.ts`'s header comment updated to stop claiming the JSON comes from `tests/contract_fixtures.py`.
- Mock-data pipeline reconnection complete: `scripts/generate_mock_data.py` rewritten, `lib/mock-data.ts` comment corrected, `package.json` `mock-data` script added, all 4 `lib/mock-data/*.json` files regenerated from real sample data (28 valid orders, 28 allocation results/1 backorder/3 supplier follow-ups, 25 aging rows/2 data issues/17 draft messages, 3 report manifests — up from 1-2 rows per table). `uv run pytest` (172), `npm run typecheck`, `npm run lint`, and `npm run build` (all 9 routes still prerender statically) all clean after regenerating.
- Phase 10 decisions (via `/grill-with-docs` [`/grilling` + `/domain-modeling`] then `/architect`, both documented in `docs/grilling/phase-10-fastapi-integration/` and `docs/architect/phase-10-fastapi-integration/`; one ADR): the backend is fully stateless — no `run_id`/job store, no persisted report artifacts (`docs/adr/0006`). `GET /api/reports/{report_id}` (documented in `build-plan.md` since Phase 0) was never implemented; replaced with three `POST .../report` endpoints that re-accept source files and recompute server-side rather than trust a client-supplied result, so the server stays the sole source of truth for anything it exports. `backend/` lives at repo root (`context/architecture.md` had already named this location). Two independent dev servers (FastAPI `:8000`, Next.js `:3000`) with explicit CORS (`allow_origins` scoped to the dev origin, `expose_headers` for `Content-Disposition`/`X-Report-Id`/`X-Generated-At` — cross-origin JS can't read headers the server doesn't explicitly expose, caught during plan review). Route handlers return the real local `TypedDict` result types (`OrderValidationResult`/`InventoryAllocationResult`/`PaymentAgingResult`) directly as return-type annotations — Pydantic v2 validates/serializes these natively, confirmed via a `TestClient` smoke test — rather than duplicating them as parallel Pydantic `BaseModel`s. Frontend state is page-local (`RequestStatus`/`currentResult`/`errorDetail` per page), not a shared hook or global store — the three workflows have different file/field shapes, and only the true mechanics (form-data POST, error parsing, blob download) are shared, in `lib/api-client.ts`. Live report downloads are plain button actions with their own `ReportRequestState`, not `ReportCard` — a live download either succeeds or fails, it isn't a registry entry. `/dashboard` and `/reports` both stay static (no cross-page store exists to aggregate their "this session's results" concept); `/reports` reframed as a sample overview rather than left implying a live report history it can't back. "Sample Template" renamed to "Sample File" everywhere, including the underlying `MissingColumnsError`/`InvalidInventoryDataError`/`InvalidOrderDataError` messages in `src/` (not just UI copy) — the existing `sample_data/*.xlsx` files carry the same intentional data-quality issues as the rest of the demo, not a blank starting point. Uploaded files must be `.xlsx` (extension mandatory, content-type advisory); corrupt-but-correctly-named files are `400`, not `500` — still user input failure. `fastapi[standard]` used as a single dependency (bundles `uvicorn[standard]`/`python-multipart`/`httpx`/`jinja2`) rather than separate pins. Mid-session addition (after the original plan): each workflow page gained a "Run sample data" action that fetches the allowlisted sample files via `fetchSampleFile()` and submits them through the exact same code path as a manual upload — no new backend endpoint, distinct from the existing "Sample file" download link.
- Phase 10 complete: `backend/{main,errors,uploads}.py`, `backend/routers/{orders,inventory,payments,templates}.py`, `lib/api-client.ts`, all 3 workflow pages live-wired (`RequestStatus`/`ReportRequestState`/"Run sample data"), `app/reports/page.tsx` reframed, `ReportCard`/`UploadPanel`/`Button` gained small additive prop extensions, `context/architecture.md` endpoint list corrected, `src/excel_io.py`/`src/inventory_allocation.py` wording fix. `uv run pytest` 194 passing (up from 172). `npm run typecheck`/`lint`/`build` clean. Live-browser verification via Playwright (project has no committed run-skill yet) confirmed all 3 workflows compute real results matching the sample data, report downloads work, the Payment Aging date field is live and editable, a deliberately malformed upload renders a correct business-readable error (not a raw exception), no console/CORS errors, and `/dashboard`/`/reports` remain visually unchanged from Phase 9.
- Phase 10.2 decisions (via `/architect`, `docs/architect/phase-10.2-portfolio-ui-polish/`; scope extended mid-implementation several times via live screenshots from the user, each addressed in place): new shared "Inverse Surface" token family (not a sidebar-only family) backs both `SidebarNav` and a new `Button` `dark` variant, so the two can't visually drift apart — active sidebar links changed from a subtle light-tint to solid `bg-accent text-text-on-accent` (a real change, not a carry-over, since the subtle tint had poor contrast against the new dark background). `MetricCard` restructured (icon → value → label, centered, min-height) rather than just retuning its grid — accepted touching all ~15 call sites since the `label`/`value`/`icon`/`tone` prop contract didn't change. Mid-session, the user redirected `/dashboard`'s KPI layout from "3 per-workflow groups, just retuned" to "one unified 5-card Overview row," an explicit content-reduction decision (dropped KPIs stay available on each workflow page's own summary) — the three workflow pages' own KPI grids were correspondingly reverted from a shared 4-column cap back to each fitting its own card count (6/5/4) in one row, a direct user correction. Chart cards gained a stable `min-h-48` body (fixing a real CSS Grid row-stretch sizing bug), a compact top-right action link (new `TableSectionHeading` `action` prop), and data-bearing hover/focus tooltips (local `useState` for the donut, pure CSS `group-hover`/`group-focus` for bars — no new charting library). Two real bugs were found and fixed independent of the requested polish: a React hydration mismatch from multi-line JSX inside an SVG `<title>`, and a `pointer-events` bug where the donut's center-total overlay silently blocked hover/focus on the ring segments beneath it. `UploadPanel`'s drop-zone misalignment (in a multi-panel row) was diagnosed as a bottom-anchoring bug, not a within-row alignment bug as the request wording suggested — reading the actual component source and a real screenshot showed the row was already correct. A pre-existing, out-of-scope limitation (no responsive sidebar collapse on mobile) was surfaced and explicitly not fixed, to avoid silently expanding a token-polish phase into a navigation-architecture change.
- Phase 10.2 complete: `app/globals.css`/`tailwind.config.ts`/`context/ui-tokens.md` (Inverse Surface tokens), `components/ui/Button.tsx` (`dark` variant), `components/layout/SidebarNav.tsx`, `components/workflow/MetricCard.tsx`, `components/workflow/UploadPanel.tsx`, `components/tables/TableSectionHeading.tsx` (`action` prop), `components/charts/{DonutBreakdownChart,VerticalBucketBarChart}.tsx`, `app/dashboard/page.tsx` (KPI consolidation, chart-card fixes, table pairing), all 3 workflow pages (KPI grid density, `dark` Download Report button). `uv run pytest` 196, unchanged (no Python touched). `npm run typecheck`/`lint`/`build` clean; raw-color/hex grep across every touched file clean. Live-browser verification via Playwright (desktop + mobile, including hover/focus states) confirmed all of the above and caught both bugs before they shipped. `context/ui-registry.md` updated for every touched component plus a new "Page composition notes (Phase 10.2)" section.
- Phase 11 pivot decision (recorded for continuity — full detail in `memory.md`'s "Phase 11 Pivot" session block): the originally planned "Phase 11: SQL Reporting + Active Sample Dashboard" (SQLite-backed) was started, partially implemented, smoke-tested, then explicitly paused by the user in favor of deploying the app first and building a Postgres-backed dashboard later instead. The working SQLite code (`backend/reporting_db/`, `backend/routers/dashboard.py`, `src/demo_constants.py`, supporting docs/ADR) was not deleted — it is preserved in `git stash@{0}` ("Phase 11 SQLite reporting WIP (paused)...") as a candidate reference for the future Phase 12 Postgres design. "Phase 11" was reassigned to Deployment Baseline; the retired SQL-reporting plan remains an accurate historical record at `/Users/jessejames/.claude/plans/yes-the-plan-aligns-smooth-barto.md` (archived into the repo as `docs/archive/phase-11-sql-reporting-sqlite-plan.md` during Phase 12), just no longer active scope. Correction made during Phase 12: this bullet previously cited `docs/adr/0007-sql-reporting-seeds-from-tested-workflow-outputs.md` as an existing file — that ADR was in fact never written (only referenced as a forward pointer in code comments) before the pause; the `0007` slot was later filled by a materially different design, `docs/adr/0007-session-scoped-workflow-result-persistence.md`.
- Phase 11 (Deployment Baseline) decisions (via `/architect`): two independent hosted services — Vercel Hobby (frontend) + Render Free Web Service (backend) — matching the existing two-server local dev shape, rather than a single containerized deployment. Explicit-promotion deploy model: a dedicated `deploy/portfolio-demo` branch is fast-forwarded to a verified commit on `phase/10-fastapi-integration` rather than auto-deploying every WIP push. No Postgres/DuckDB/SQLite/auth/persisted-uploads/run-history in this phase — strictly a public-URL baseline on top of the already-stable Phase 10.2 app. `.env.example` stays an empty template in git; real deployed values live only in the Vercel/Render dashboard configs and in `README.md`'s Live Demo section, never committed together in one file. Render's Pre-Deploy Command field (gated behind a payment method on free instances) was left untouched since this app has no migration/asset-upload step needing it; Render's one-time card-verification hold on "Deploy Web Service" was confirmed via web search as a known real anti-abuse practice, not a misconfiguration.
- Phase 11 complete: `backend/main.py` (env-driven CORS + `/health`), `.env.example` (new) + `.gitignore` fix, `README.md` Live Demo section, `context/build-plan.md`/`context/architecture.md` (two-service deployment shape), `CLAUDE.md` scope-pointer correction — landed in commits `6b6a716` and `974b348` on `phase/10-fastapi-integration` (updating existing PR #9), on top of Phase 10.2's UI polish work (commit `a2615cc`, previously uncommitted). Deployed and live-verified: backend on Render (https://sales-ops-6e84.onrender.com), frontend on Vercel (https://sales-ops-gamma.vercel.app/), both served from `deploy/portfolio-demo` at commit `974b348`. `uv run pytest` 196, unchanged. Verified live via direct curl: `/health` 200, root redirect + real rendered dashboard content, and a correct CORS preflight response (after catching and fixing a trailing-slash mismatch in the configured origin before it could break in production).
- Mobile Nav/Shell Responsiveness decisions (via `/architect`, one review/correction round before approval): resolves the Phase 10.2-flagged limitation (fixed `w-60` sidebar squeezing mobile content) now that the app is live in production. Breakpoint fixed at `lg` (1024px), matching the app's existing `lg`-based responsive convention elsewhere (workflow-page KPI grids) rather than introducing a `md` cutoff. New `--overlay` token added (reusing `--surface-inverse`'s HSL triple) for the drawer backdrop — a real token-change decision per `ui-tokens.md`'s rule, not a raw Tailwind color or inline opacity hack. Hand-rolled drawer, no headless dialog dependency, matching this project's established preference for hand-writing primitives against its own tokens (the same reasoning that kept shadcn out back in Phase 8/9). `AppShell` and `TopHeader` both promoted from Server to Client Components — `AppShell` to own `isDrawerOpen` state plus `usePathname`/`Escape`-key effects, `TopHeader` because it now accepts an `onToggle` callback and renders the interactive menu button. **Two separate wrapper elements, not one dual-purpose wrapper** (a direct user correction to an earlier draft): a desktop wrapper (`hidden lg:block`, always mounted, unchanged behavior) and a mobile drawer wrapper that's conditionally rendered only while open — chosen specifically over a permanently-mounted `-translate-x-full` panel, because that would leave the closed drawer's nav links reachable via Tab even while invisible. No `role="dialog"`/`aria-modal` claimed on the drawer (another user correction) since there's no real focus trap to back that up — `SidebarNav`'s own `<nav>` landmark provides real semantics instead. Trade-off accepted: no animated slide transition in this pass (instant mount/unmount) since conditional mounting for the accessibility fix and a smooth bidirectional transition don't compose without extra timing logic; a future pass could add one. Tracked as a named fix, not a numbered phase — Phase 12 stays reserved for the Postgres dashboard.
- Mobile Nav/Shell Responsiveness complete: `app/globals.css`/`tailwind.config.ts`/`context/ui-tokens.md` (new `--overlay` token), `components/layout/AppShell.tsx` (Client Component, drawer state, two wrappers), `components/layout/TopHeader.tsx` (Client Component, `lg:hidden` menu toggle), `components/layout/SidebarNav.tsx` unchanged. `npm run typecheck`/`lint`/`build` clean. Live-browser verification at mobile and desktop widths confirmed: mobile drawer opens/closes via toggle/backdrop/Escape/route-change, main content reaches full width when closed, desktop sidebar unchanged; a keyboard-only pass confirmed nav links are unreachable via Tab while the drawer is closed and reachable once open. `context/ui-registry.md` updated (`AppShell`/`SidebarNav`/`TopHeader` entries + new page composition notes section), Phase 10.2's known-limitation note marked resolved. `docs/plan/mobile-nav-shell-responsiveness/` written via `/feature-docs`; `docs/architect/mobile-nav-shell-responsiveness/` written via `/session-docs`.
- Phase 12 decisions (via an extensive `/grill-with-docs` session, 13 resolved decision points, plus `/domain-modeling`): Anonymous Session ID lives in `localStorage` only, sent as `X-Session-Id` — no cookies, chosen specifically to avoid `SameSite=None`/`allow_credentials` cross-site-cookie complexity between Vercel and Render. Persistence is folded directly into the three existing workflow endpoints (not a separate save endpoint, which would either trust a client-resubmitted result or force a redundant re-upload) and is strictly best-effort — a DB write failure never fails a request that already computed a valid result, always reported via `X-Persisted` (`true`/`false`/`skipped`), never silent. `POST /api/inventory/allocate` persists only `inventory_allocation`, never the `validate_orders()` byproduct it computes internally — the user never invoked or received a validation workflow in that request. Storage is one JSONB table (verbatim Output Contract per row), not a normalized SQL-reporting schema like the paused SQLite design — the read pattern is "latest result for this session," never cross-session SQL analytics. `psycopg` 3 + hand-written SQL (no ORM), matching this project's established precedent; hand-written migrations (no Alembic) run as one transaction in the FastAPI lifespan hook, advisory-lock-guarded and checksum-verified, with a bounded cold-start retry (kept under Render's health-check timeout) distinguishing "`DATABASE_URL` set but unreachable at boot" (fail-closed startup) from "unset" (persistence cleanly disabled, not an error) from a post-startup runtime outage (`GET /api/dashboard` → `503`, distinct from the normal `200` all-`null` "nothing saved yet" case). The dashboard's session-aware sections are a Client Component fetching on mount, not a Server Component fetch — a direct, necessary consequence of the no-cookies decision, since a Vercel-side render has no access to `localStorage`; this explicitly retires the *paused* SQLite plan's old "async Server Component + `force-dynamic`" assumption, which depended on a non-session-scoped query that no longer describes this design. `X-Persisted: false` is surfaced in the UI only as a small non-blocking note (not `BusinessErrorMessage`) and only on `false` — `true`/`skipped` render nothing, matching this project's existing no-success-chrome convention. Neon branching (`main`/`dev`/`test`) with a two-layer test suite (mocked-repo route tests always run; real-Neon repository tests marked `@pytest.mark.db` and skipped without `TEST_DATABASE_URL`, a separate env var from `DATABASE_URL` specifically so a misconfigured environment can never point a cleanup-driven test at `dev` or `main`) — chosen over Docker (no new infra needed since Neon is already Postgres) and over pure mocking (would leave the real SQL entirely unverified). TTL is read-time display expiry only (rows hidden from the dashboard after 30 days, never actively deleted in Phase 12) — an explicit, named distinction from a real deletion/privacy guarantee.
- Phase 12 correctness fixes found via a second review pass before implementation (folded into the plan before `ExitPlanMode`): `saved_at = now()` must be explicit in the upsert's `DO UPDATE SET` clause, not left to a column `DEFAULT` (which only fires on `INSERT`) — otherwise Display Expiry would measure age from a session's first-ever save, not its latest. `schema_migrations` is owned by the migration runner, never bootstrapped inside a numbered migration file. `json.dumps(..., allow_nan=False)` guards the JSONB write against `NaN`/`Infinity` (which pandas can produce and Postgres's `JSONB` type rejects), failing loudly as an ordinary `X-Persisted: false` rather than silently and permanently. A missing `DATABASE_URL` must not fail app startup or any test — required for `uv run pytest`'s hermeticity to hold, since the lifespan hook runs even under `TestClient`. The repository is exposed as a FastAPI dependency (`app.dependency_overrides`-friendly), not a bare module function, for clean test injection. One correctness gap was found only while writing the route-orchestration tests themselves (not during planning): the three routers originally called `repo.save()` with no `try/except`, trusting the repository's own "never raises" contract — a mocked repo configured to raise would have produced a `500`, contradicting the best-effort guarantee. Fixed by extracting a shared `backend/persistence.py::persist_workflow_result` helper with its own defensive `try/except`, used by all three routers.
- Phase 12 complete: `docs/adr/0007-session-scoped-workflow-result-persistence.md`, `CONTEXT.md` (7 new/amended terms), `docs/archive/phase-11-sql-reporting-sqlite-plan.md` (new — archives the superseded SQLite plan for future study). Backend: `backend/migrations/0001_create_workflow_results.sql`, `backend/db.py`, `backend/session.py`, `backend/repositories/workflow_results.py`, `backend/persistence.py`, `backend/routers/dashboard.py`, `backend/main.py` (lifespan + CORS), `orders.py`/`inventory.py`/`payments.py` routers, `src/contracts.py` (`CONTRACT_SCHEMA_VERSIONS`). Frontend: `lib/session-id.ts`, `lib/api-client.ts`, `types/dashboard.ts`, `components/feedback/PersistenceNotice.tsx`, `components/dashboard/DashboardLiveSections.tsx`, `app/dashboard/page.tsx` (trimmed), `components/workflow/MetricCard.tsx` (`sample` prop), all 3 workflow pages. Tests: `tests/test_backend_persistence_routing.py` (15, mocked, always run), `tests/test_workflow_results_repository.py` (8, `@pytest.mark.db`, real Neon). `uv run pytest`: 212 passing, 7 skipped, fully hermetic with no env vars set; the same 8 db-marked tests pass green against the real Neon `test` branch (left clean afterward, 0 residual rows) when `TEST_DATABASE_URL` is set. `npm run typecheck`/`lint`/`build` clean; `/dashboard` still statically prerenders. Verified live against the real Neon `dev` branch (migration application, save/read/upsert-latest-wins/`saved_at`-advances/schema-version-gate all directly confirmed) and via a real-browser Playwright pass (fresh-visitor full sample fallback with chips; running a workflow then reloading the dashboard shows that section live with the chip gone, other sections still sample; `localStorage.clear()` returns to full sample state). CORS `X-Persisted` exposure confirmed via curl against a live cross-origin request. A real secret-leak near-miss was caught mid-session (real Neon credentials briefly written to the committed `.env.example` instead of gitignored `.env`) and fixed before anything was staged — flagged to the user directly as a going-forward rule.
